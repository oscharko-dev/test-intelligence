# Block dependency lifecycle (install) scripts as a supply-chain
# hardening measure. Consequence: this package's own `prepare` script
# does not run on `pnpm install`, so the Git hooks in `.githooks/` are
# not auto-enabled. Contributors enable them once with `pnpm run prepare`.
ignore-scripts=true

# Pin exact dependency versions so installs are reproducible.
save-exact=true
