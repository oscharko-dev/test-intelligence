import type { ReactNode } from "react";
import { SettingsScreen } from "@/components/settings/SettingsScreen";

export default function ModelSettingsPage(): ReactNode {
  return <SettingsScreen />;
}
