/**
 * Bootstrap-period re-export shim for branded ID helpers and types.
 * The canonical module is `packages/contracts/src/branded-ids.ts` per
 * ADR-0010. See `./index.ts` for the rationale and retirement plan.
 */

export * from "@oscharko-dev/ti-contracts";
