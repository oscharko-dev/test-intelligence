{
  "name": "@oscharko-dev/ti-contracts",
  "version": "0.1.0-beta.4",
  "description": "Public contract surface: schemas, branded IDs, version constants.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b && tsc -p tsconfig.test.json --noEmit",
    "lint": "node -e \"process.exit(0)\"",
    "test": "tsx --test src/branded-ids.test.ts src/branded-ids.compile-failure.test.ts src/contract-version.test.ts src/submit-mode-parity.test.ts src/evidence-seal-compat.test.ts"
  }
}
